- name: Include Azure resource manager API versions
  include_vars:
    file: resource-provider-apis.yml

- name: Generate region naming and tagging variables
  include_vars:
    file: region.yml

- name: Generate environment naming and tagging variables
  include_vars:
    file: environment.yml

- name: Generate division naming and tagging variables
  include_vars:
    file: division.yml

- name: Generate service naming and tagging variables
  include_vars:
    file: service.yml
